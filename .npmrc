# NPM Configuration for Cloud Build
# Ensure proper package installation in CI environments

# Install dev dependencies during build
include=dev

# Use npm ci for faster, reliable installs
engine-strict=true

# Disable package-lock updates in CI
package-lock=false

# Optimize for CI environments
audit=false
fund=false
progress=false
